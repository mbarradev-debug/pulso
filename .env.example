# Credenciales de la API SI3 del Banco Central de Chile (https://si3.bcentral.cl).
# Server-only: se leen exclusivamente en Route Handlers / lib server-side.
# Nunca prefijar con NEXT_PUBLIC_ (eso las expondría al navegador).
BCENTRAL_USER=
BCENTRAL_PASS=
